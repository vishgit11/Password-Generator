package allCharacters;

public interface AllCharacters {

    void randomAlpha(int randomIndex);
    void randomNumber(int randomIndex);
    void randomSymbol(int randomIndex);

    String getPassward();
}
